# LeadScout Production Environment Variables
# Copy this file and replace all REPLACE_WITH_* values with actual production credentials

# ===========================================
# Node.js Environment
# ===========================================
NODE_ENV=production

# ===========================================
# Application Configuration
# ===========================================
# Your production domain (e.g., https://leadscout.app or https://leadscout-production.ondigitalocean.app)
NEXT_PUBLIC_APP_URL=REPLACE_WITH_PRODUCTION_URL

# ===========================================
# Clerk Authentication
# ===========================================
# Get from: https://dashboard.clerk.com > Your App > API Keys
# IMPORTANT: Use LIVE keys (pk_live_* and sk_live_*) for production
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=REPLACE_WITH_CLERK_LIVE_PUBLISHABLE_KEY
CLERK_SECRET_KEY=REPLACE_WITH_CLERK_LIVE_SECRET_KEY

# Clerk redirect URLs (update if using custom domain)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding

# ===========================================
# Convex Backend
# ===========================================
# Get from: npx convex deploy --prod
# Production deployment URL will be shown after deployment
NEXT_PUBLIC_CONVEX_URL=REPLACE_WITH_PRODUCTION_CONVEX_URL

# Get from: Convex Dashboard > Settings > Deploy Keys
CONVEX_DEPLOY_KEY=REPLACE_WITH_CONVEX_DEPLOY_KEY

# ===========================================
# Stripe Payments
# ===========================================
# Get from: https://dashboard.stripe.com > Developers > API keys
# CRITICAL: Use LIVE keys (pk_live_* and sk_live_*) for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=REPLACE_WITH_STRIPE_LIVE_PUBLISHABLE_KEY
STRIPE_SECRET_KEY=REPLACE_WITH_STRIPE_LIVE_SECRET_KEY

# Stripe Webhook Secret
# Get from: Stripe Dashboard > Developers > Webhooks > Your endpoint
# After creating webhook endpoint, copy the signing secret
STRIPE_WEBHOOK_SECRET=REPLACE_WITH_STRIPE_WEBHOOK_SECRET

# Stripe Product Price IDs
# Create products in: Stripe Dashboard > Products
# Copy the Price ID (starts with price_) for each product
NEXT_PUBLIC_STRIPE_STARTER_PRICE_ID=REPLACE_WITH_STARTER_PLAN_PRICE_ID
NEXT_PUBLIC_STRIPE_GROWTH_PRICE_ID=REPLACE_WITH_GROWTH_PLAN_PRICE_ID
NEXT_PUBLIC_STRIPE_SCALE_PRICE_ID=REPLACE_WITH_SCALE_PLAN_PRICE_ID

# Credit top-up price (one-time payment)
STRIPE_CREDIT_PRICE_ID=REPLACE_WITH_CREDIT_PRICE_ID

# ===========================================
# Resend Email Service
# ===========================================
# Get from: https://resend.com/api-keys
RESEND_API_KEY=REPLACE_WITH_RESEND_API_KEY

# Verified sending email (must be verified in Resend dashboard)
# Format: noreply@yourdomain.com
RESEND_FROM_EMAIL=REPLACE_WITH_VERIFIED_EMAIL

# ===========================================
# Business Logic Configuration
# ===========================================
# Minimum payout amount in euros
PAYOUT_MINIMUM_THRESHOLD=20

# Platform commission rate (0.5 = 50%)
PLATFORM_COMMISSION_RATE=0.5

# Monthly credits per subscription plan
STARTER_PLAN_CREDITS=20
GROWTH_PLAN_CREDITS=60
SCALE_PLAN_CREDITS=150

# ===========================================
# GitHub Actions Secrets
# ===========================================
# These should be set in GitHub repository settings > Secrets and variables > Actions

# Digital Ocean Access Token
# Get from: https://cloud.digitalocean.com/account/api/tokens
# DIGITALOCEAN_ACCESS_TOKEN=REPLACE_WITH_DO_TOKEN

# All the above environment variables should also be added as GitHub secrets
# for CI/CD pipeline to work correctly
